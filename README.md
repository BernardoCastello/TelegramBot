# TelegramBot
Telegram bot to answer professional questions about me.


# To start for the first time

*Start a venv environment with python*

python -m venv venv
venv\Scripts\activate (Windows)
source venv/bin/activate (macOS/Linux)

*Install dependencies*

pip install -r requirements.txt

*Start Docker*
docker compose up -d
